// src/components/FolderListValidation.js

/**
 * Fonction de validation pour la confirmation de suppression.
 * @param {boolean} confirm - Confirmation de l'utilisateur.
 * @returns {boolean} Résultat de la validation.
 */
export const validateDeleteConfirmation = (confirm: boolean) => {
  return confirm;
};

// Vous pouvez ajouter d'autres validations spécifiques ici si nécessaire.
