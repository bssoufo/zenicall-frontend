/* src/components/Documents/DocumentImage.css */

.document-image-container {
    width: 100%;
    max-height: 500px;
    overflow: hidden; /* Pour éviter que l'image ne dépasse son container */
    border: 1px solid #ccc;
    border-radius: 4px;
    display: flex;
    justify-content: center; /* Centrer l'image horizontalement */
    align-items: center; /* Centrer l'image verticalement */
    padding: 10px; /* Ajout de padding autour de l'image */
    box-sizing: border-box; /* Assurer que le padding n'augmente pas la taille du conteneur */
  }
  
  .document-image {
    max-width: 100%;
    max-height: 100%; /* Assurer que l'image ne dépasse pas le conteneur en hauteur */
    object-fit: contain; /* Adapter l'image au conteneur sans la déformer */
    display: block; /* Élimine l'espace supplémentaire en bas de l'image */
  }
  
  /* Responsive design */
  @media (max-width: 768px) {
    .document-image-container {
      max-height: 300px;
      padding: 5px;
    }
  }