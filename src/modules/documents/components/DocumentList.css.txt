/* src/components/Documents/DocumentList.css */

.document-list {
    padding: 20px;
  }
  
  .document-list-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    margin-bottom: 20px;
  }
  
  .add-button {
    margin-left: 10px;
    padding: 8px 12px;
    background-color: #2ecc71;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
  }
  .add-button:hover {
    background-color: #27ae60;
  }
  
  .status-filter {
    display: flex;
    align-items: center;
    margin-top: 10px;
  }
  .status-filter label {
    margin-right: 10px;
    color: #2c3e50;
    font-weight: 600;
  }
  .status-filter select {
    padding: 8px;
    font-size: 14px;
    border: 1px solid #ccc;
    border-radius: 4px;
    box-sizing: border-box;
  }
  .status-filter select:focus {
    outline: none;
    border-color: #3498db;
    box-shadow: 0 0 5px rgba(52, 152, 219, 0.5);
  }
  
  /* Boutons vue / édition */
  .view-button,
  .edit-button {
    margin-right: 10px;
    padding: 5px 10px;
    color: white;
    text-decoration: none;
    border-radius: 3px;
  }
  
  .view-button {
    background-color: #3498db;
  }
  .edit-button {
    background-color: #f1c40f;
  }
  .view-button:hover {
    background-color: #2980b9;
  }
  .edit-button:hover {
    background-color: #f39c12;
  }
  
  /* Message d’erreur */
  .error {
    color: red;
  }
  
  /* Responsive Design */
  @media (max-width: 768px) {
    .document-list-header {
      flex-direction: column;
      align-items: flex-start;
    }
    .status-filter {
      margin-top: 10px;
    }
  }
  
  .document-list-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
  }
  
  .add-button,
  .process-button {
    padding: 10px 20px;
    background-color: #28a745; /* Vert pour ajouter */
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    transition: background-color 0.3s;
    display: flex;
    align-items: center;
  }
  
  .add-button:hover,
  .process-button:hover {
    background-color: #218838;
  }
  
  .process-button {
    background-color: #007bff; /* Bleu pour processer */
  }
  
  .process-button:hover {
    background-color: #0069d9;
  }
  
  .process-button:disabled,
  .add-button:disabled,
  .status-filter select:disabled {
    background-color: #6c757d;
    cursor: not-allowed;
  }
  
  .document-list-controls {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
  }
  
  .status-filter {
    display: flex;
    align-items: center;
  }
  
  .status-filter label {
    margin-right: 10px;
  }
  
  .status-filter select {
    padding: 8px 12px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 16px;
  }
  
  .paginated-list {
    /* Vos styles existants */
  }
  
  .spinner {
    margin-left: 8px;
    /* Les spinners FontAwesome ont déjà une animation de rotation via la prop 'spin' */
  }
  
  .large-spinner {
    font-size: 24px;
    margin-bottom: 10px;
  }
  
  .processing-indicator {
    display: flex;
    align-items: center;
    margin-bottom: 20px;
    color: #007bff;
  }
  
  .error-message {
    color: #dc3545;
    margin-bottom: 20px;
  }

  