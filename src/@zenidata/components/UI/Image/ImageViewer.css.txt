.image-viewer {
    display: flex;
    flex-direction: column;
    align-items: center; /* Center items horizontally */
    justify-content: center;/* Center items vertically */
    overflow: hidden; /* To prevent overflow from dragged image */
  }
  
  .image-viewer img {
    max-width: 500px;
    display: block; /* Remove extra space below the image */
    cursor: grab;
    transition: transform 0.3s ease; /* Optional for smooth transitions */
  }
  
  .image-controls {
    display: flex;
    justify-content: center; /* Center buttons */
    margin-top: 10px;
  }
  
  .image-controls button {
    margin: 0 5px;
    padding: 8px 12px;
    background-color: #4CAF50;
    color: white;
    border: none;
    cursor: pointer;
    z-index: 9000;
  }

  .image-viewer {
    position: relative;
    overflow: hidden; /* Hide parts of the image that move outside the container */
    width: 100%;
    height: 100%;
  }
  
  .image-viewer img {
    user-select: none; /* Prevent text/image selection during drag */
    -webkit-user-drag: none; /* Disable default image dragging behavior */
    touch-action: none;    /* Disable default touch behaviors to allow custom panning */
  }
  
  .image-controls {
    position: absolute;
    bottom: 10px;
    left: 10px;
    /* Additional styling for controls as needed */
  }
  
  /* Optional: Visual feedback for dragging state */
  .image-viewer img.dragging {
    opacity: 0.8;
    /* Additional styles like box-shadow can be added here */
  }
  